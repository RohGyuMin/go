# CLAUDE.md — AI 협업 시스템 운영 규칙

이 저장소는 AI 지원 개발에서 "작성 속도"와 "팀의 이해 속도"의 균형을 맞추기 위한 시스템입니다.
Claude Code(및 동료 개발자)는 아래 규칙을 따릅니다.

## 축 1 — 설명 문서 + 퀴즈

의미 있는 변경(기능 추가/리팩터링/버그 수정)을 마무리할 때:

1. **종합 설명 문서를 생성한다.** 단순 diff 나열이 아니라 아래를 포함한다.
   - **배경/맥락**: 이 변경이 필요한 이유, 전체 시스템에서의 위치
   - **직관적 개요**: 비개발자도 감을 잡을 수 있는 한 문단 요약, 필요 시 다이어그램
   - **작성 의도**: 왜 이렇게 설계했는지, 고려한 대안과 트레이드오프
   - **변경의 파급 효과**: 어떤 부분이 영향을 받는지
   - 산출물 위치: `docs/explanations/<YYYY-MM-DD>-<slug>.md` (템플릿: `templates/explanation.md`)
2. **설명 문서 하단에 5문항 중간 난이도 퀴즈를 생성한다.** 정답과 해설을 접이식(details)으로 포함한다.
   단순 암기가 아니라 "왜/어떻게/만약에"를 묻는다.
3. **자가검증 규칙을 적용한다** (아래 참조).

`/explain-diff` 슬래시 커맨드가 위 과정을 자동화한다.

## 축 2 — 마이크로월드

시스템의 동적 동작(상태 전이, 이벤트 흐름, 알고리즘)을 이해해야 할 때, 텍스트 코드를 정독하는 대신
`templates/micro-world/`를 복제해 **타임라인 스크럽이 가능한 시각적 디버거**를 만든다.
코드에 추적 로그(trace)를 심어 상태 스냅샷을 JSON으로 남기고, 그 JSON을 마이크로월드로 재생한다.
`/micro-world` 커맨드가 스캐폴딩을 자동화한다.

## 축 3 — 공유 공간 연동

혼자 AI와 대화하는 1:1 루프에 머무르지 않는다. 설계 결정·설명 문서·계획은
팀 공유 공간(Notion/Slack)에 올려 동료와 AI가 함께 댓글·리뷰하도록 한다.
`/share-to-space` 커맨드가 요약을 공유 공간으로 내보낸다.

---

## 🚦 자가검증 규칙 (Self-Verification Gate) — 필수

> **개발자 본인이 자신의 변경에 대한 AI 생성 퀴즈(5문항)를 통과하지 못하면,
> 코드를 코드베이스에 반영하지 않고 동료에게 리뷰를 요청한다.**

- 통과 기준: 5문항 중 **4문항 이상** 정답 (팀 상황에 맞게 조정 가능).
- 통과 시: 설명 문서 링크를 PR에 첨부하고 병합을 진행한다.
- 미통과 시: 스스로 이해가 부족하다는 신호이므로 (a) 설명 문서를 다시 읽고 (b) 동료 리뷰를 요청한다.
- 목적: "AI가 짜줬으니 됐다"가 아니라 "내가 이해한 만큼만 내 이름으로 병합한다"는 문화를 만든다.

이 게이트는 사람이 정직하게 지키는 규칙이며, CI는 **설명 문서·퀴즈의 존재 여부**만 강제한다
(사람의 이해도 자체는 자동 검증할 수 없다). `.github/workflows/explanation-check.yml` 참조.
